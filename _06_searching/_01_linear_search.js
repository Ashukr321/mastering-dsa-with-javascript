// 1. Linear Search

/**
 * Pattern Used: Linear Search (Brute-Force Sequential Scan)
 * Why this pattern: Walks through every element one by one comparing it to the
 * target, making no assumption about ordering, so it works on any array but costs O(n).
 *
 * Interview Scenario:
 * "Baseline warm-up question interviewers use before asking you to optimize:
 * find a value in an unsorted array of employee IDs and explain why you can't
 * do better than O(n) without additional structure or sorting."
 *
 * Example:
 *   Input:  arr = [5, 3, 8, 1, 9], target = 8
 *   Output: 2 (index of 8)
 */
