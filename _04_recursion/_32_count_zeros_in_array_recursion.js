// 32. Count the number of Zero in the array using recursion
/**
 * Pattern Used: Linear Recursion with Conditional Counting
 * Why this pattern: Combines array traversal recursion with a conditional check (element === 0) at each step, adding 1 only when the predicate matches, similar in spirit to the even/odd sum filtering problems but applied to arrays.
 *
 * Interview Scenario:
 * "Used to test whether a candidate can combine simple predicate checks with recursive traversal on arrays, a pattern that generalizes directly to 'count occurrences of target X in array' questions."
 *
 * Example:
 *   Input:  arr = [0, 4, 0, 0, 7, 2]
 *   Output: 3
 */
