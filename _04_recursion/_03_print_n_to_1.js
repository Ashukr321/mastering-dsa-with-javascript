// 3. Print the number n to 1 using recursion
/**
 * Pattern Used: Recursion - Base Case + Recurrence (Pre-order print / work before recursive call)
 * Why this pattern: The value is printed before the recursive call is made, so the sequence naturally comes out in descending order without needing any extra state.
 *
 * Interview Scenario:
 * "Paired with _01 in interviews to test if the candidate understands the difference between 'print before call' vs 'print after call' - a common follow-up at Microsoft-style interviews."
 *
 * Example:
 *   Input:  n = 5
 *   Output: 5 4 3 2 1
 */
