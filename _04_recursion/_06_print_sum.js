// 6. Print the sum
/**
 * Pattern Used: Head Recursion (accumulate through parameters, print after unwinding)
 * Why this pattern: This variant threads a running total through recursive calls and prints it only once the base case is reached, illustrating how accumulator-style recursion differs from simple return-value recursion.
 *
 * Interview Scenario:
 * "Used as a follow-up to the basic sum question to see if candidates can convert an accumulator-based iterative loop into an equivalent recursive helper function."
 *
 * Example:
 *   Input:  n = 4
 *   Output: 10
 */
