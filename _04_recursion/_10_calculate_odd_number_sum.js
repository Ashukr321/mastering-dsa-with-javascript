// 10. Calculate the odd number sum
/**
 * Pattern Used: Recursion with Conditional Filtering
 * Why this pattern: Mirrors the even-sum problem but filters on odd numbers, reinforcing that the same recursive skeleton can be reused by simply swapping the predicate.
 *
 * Interview Scenario:
 * "A common variation question interviewers ask right after the even-sum problem to see if the candidate generalizes the pattern instead of rewriting it from scratch: 'now do the same for odd numbers'."
 *
 * Example:
 *   Input:  n = 10
 *   Output: 25  (1+3+5+7+9)
 */
