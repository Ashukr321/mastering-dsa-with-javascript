// 9. Print the sum of even number using recursion
/**
 * Pattern Used: Recursion with Conditional Filtering
 * Why this pattern: The recursive step conditionally adds the current number to the running sum only when it satisfies the even-number predicate, showing how filtering logic composes with recursion.
 *
 * Interview Scenario:
 * "Used in interviews to check if a candidate can combine a conditional filter with recursive traversal instead of just summing everything, e.g. 'find the sum of all even numbers from 1 to n recursively'."
 *
 * Example:
 *   Input:  n = 10
 *   Output: 30  (2+4+6+8+10)
 */
