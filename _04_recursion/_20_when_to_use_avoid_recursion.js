// 20. When to use recursion when to avoid to use recursion
/**
 * Pattern Used: Conceptual Notes (Theory) - Trade-off Analysis
 * Why this pattern: This file discusses trade-offs (readability vs. stack depth, call overhead, tail-call optimization support) instead of implementing an algorithm, so it documents decision criteria rather than a coding pattern.
 *
 * Interview Scenario:
 * "A common senior-level interview discussion question: 'When would you avoid recursion in production code, and how would you convert a recursive solution to an iterative one to avoid stack overflow?'"
 *
 * Example:
 *   Input:  Scenario: deeply nested recursion on large input (e.g. n = 10^6)
 *   Output: Explanation of stack overflow risk and iterative/tail-call alternatives
 */
