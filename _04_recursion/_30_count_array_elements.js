// 30. Count the array element using recursion
/**
 * Pattern Used: Linear Recursion over Array (Shrink Input by One Element per Call)
 * Why this pattern: A second take on counting array elements recursively (often via index-based recursion instead of slicing), reinforcing that the same problem can be solved with different recursive traversal strategies.
 *
 * Interview Scenario:
 * "Used to test whether a candidate can implement the same recursive idea using an index pointer for O(1) extra space per call instead of creating new sub-arrays each time."
 *
 * Example:
 *   Input:  arr = [10, 20, 30, 40]
 *   Output: 4
 */
