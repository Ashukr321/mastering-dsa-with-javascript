// 4. Find the factorial of the number using recursion
/**
 * Pattern Used: Linear/Multiplicative Recursion
 * Why this pattern: Factorial has the natural recursive definition n! = n * (n-1)!, with 0! = 1 as the base case, making it the canonical first example of recursion in any interview.
 *
 * Interview Scenario:
 * "Asked at almost every entry-level interview (Amazon SDE-1, TCS, Wipro) as a warm-up to see if the candidate can identify the base case and recurrence relation correctly, and often followed by 'what if n is negative?'."
 *
 * Example:
 *   Input:  n = 5
 *   Output: 120
 */
