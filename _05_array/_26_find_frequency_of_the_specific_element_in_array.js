// 26. Find frequency of the specific element in array
/**
 * Pattern Used: Linear Scan / Counting
 * Why this pattern: Counting how many times one specific target value appears only requires a single pass with a counter, without needing a full frequency map.
 *
 * Interview Scenario:
 * "Asked in quick coding rounds at service companies: 'Count how many times a specific error code appears in a log array.' Tests targeted counting versus building an unnecessary full frequency table."
 *
 * Example:
 *   Input:  arr = [1,2,1,3,1,4], target = 1
 *   Output: 3
 */
