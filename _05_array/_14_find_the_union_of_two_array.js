// 14. Find the union of two array
/**
 * Pattern Used: Hashing / Set Union
 * Why this pattern: A hash set (or map) naturally deduplicates while combining elements from both arrays, giving the union in linear time.
 *
 * Interview Scenario:
 * "Asked at Infosys/Wipro service-based interviews: 'Merge two arrays of subscriber lists into a single deduplicated list.' Tests understanding of set semantics implemented via arrays/objects."
 *
 * Example:
 *   Input:  arr1 = [1,2,3], arr2 = [2,3,4]
 *   Output: [1,2,3,4]
 */
