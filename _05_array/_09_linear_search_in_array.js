// 9. Linear search in array
/**
 * Pattern Used: Linear Search
 * Why this pattern: Without any ordering guarantee, the only way to find a target is to scan every element sequentially until a match is found.
 *
 * Interview Scenario:
 * "Basic screening question for freshers: 'Find whether a given product ID exists in this unsorted inventory list.' Confirms fundamentals before discussing binary search trade-offs."
 *
 * Example:
 *   Input:  arr = [4, 2, 7, 1, 9], target = 7
 *   Output: 2 (index)
 */
