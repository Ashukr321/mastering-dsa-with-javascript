// 7. Even or odd
/**
 * Pattern Used: Modulo Arithmetic (Divisibility Rule)
 * Why this pattern: A number's parity is determined in O(1) by checking the remainder of division by 2
 * (`n % 2 === 0` means even), the simplest possible divisibility test.
 *
 * Interview Scenario:
 * "A sanity-check warm-up question interviewers use to see how a candidate reasons about negative numbers and
 * zero when using the modulo operator, before moving to harder numeric problems."
 *
 * Example:
 *   Input:  7
 *   Output: "Odd"
 */
