// 12. Greatest of two numbers
/**
 * Pattern Used: Direct Comparison
 * Why this pattern: A single ternary/if comparison between the two values determines the larger one in O(1),
 * with no iteration required.
 *
 * Interview Scenario:
 * "A trivial building block often embedded inside bigger problems (e.g. finding max element while scanning an
 * array) -- interviewers use it to confirm basic comparison and ternary-operator fluency."
 *
 * Example:
 *   Input:  a = 10, b = 25
 *   Output: 25
 */
