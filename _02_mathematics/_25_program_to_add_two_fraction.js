// 25. Program to add two fractions
/**
 * Pattern Used: Fraction Arithmetic via LCM/GCD Normalization
 * Why this pattern: Two fractions are added by finding a common denominator (typically the LCM of the
 * denominators), scaling numerators accordingly, summing them, and simplifying the result using the GCD.
 *
 * Interview Scenario:
 * "A rational-number arithmetic question that tests whether a candidate can combine LCM (for the common
 * denominator) and GCD (for simplifying the final fraction) into a correct, reduced result."
 *
 * Example:
 *   Input:  1/2 + 1/3
 *   Output: 5/6
 */
