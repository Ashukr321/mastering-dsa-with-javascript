// 27. Calculate the area of a circle
/**
 * Pattern Used: Direct Formula Application
 * Why this pattern: The area is computed in O(1) by directly applying the geometric formula `π * r^2`, with no
 * iteration or branching needed beyond validating that the radius is non-negative.
 *
 * Interview Scenario:
 * "A simple formula-application warm-up interviewers use to confirm a candidate handles floating-point precision
 * and input validation (e.g. negative radius) before moving to algorithmically richer problems."
 *
 * Example:
 *   Input:  r = 3
 *   Output: 28.27 (approximately)
 */
