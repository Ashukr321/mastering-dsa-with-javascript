// 24. Write a program to sort the character in the string

/**
 * Pattern Used: Sorting
 * Why this pattern: Converting the string to an array of characters and applying a comparison-based sort orders them in O(n log n), useful as a building block for anagram checks.
 *
 * Interview Scenario:
 * "Often used at Wipro/Cognizant as a lead-in to anagram detection ('sort and compare'), testing basic sort API usage on strings."
 *
 * Example:
 *   Input:  str = "edcba"
 *   Output: "abcde"
 */
