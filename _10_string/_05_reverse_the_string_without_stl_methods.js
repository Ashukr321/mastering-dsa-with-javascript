// 5. Reverse the string without stl methods
// LeetCode: https://leetcode.com/problems/reverse-string/

/**
 * Pattern Used: Two Pointers / String Reversal In-place
 * Why this pattern: Without relying on built-in reverse helpers, swapping characters from the start and end index toward the middle reverses the string manually in O(n) time.
 *
 * Interview Scenario:
 * "A go-to interview question at Microsoft and product-based startups to check if a candidate can implement basic algorithms without relying on library functions."
 *
 * Example:
 *   Input:  str = "world"
 *   Output: "dlrow"
 */
