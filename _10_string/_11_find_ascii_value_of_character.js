// 11. Find Ascii value of character

/**
 * Pattern Used: ASCII/Character Manipulation
 * Why this pattern: charCodeAt (or an equivalent) directly exposes the underlying ASCII/Unicode code point of a character, the basis for many character-arithmetic tricks.
 *
 * Interview Scenario:
 * "Tests whether a candidate knows how to move between characters and their numeric codes, a prerequisite for tricks like case conversion or Caesar ciphers."
 *
 * Example:
 *   Input:  char = 'A'
 *   Output: 65
 */
