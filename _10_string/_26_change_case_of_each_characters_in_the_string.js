// 26. Change case of each characters in the string

/**
 * Pattern Used: ASCII/Character Manipulation
 * Why this pattern: Toggling each character's case via ASCII arithmetic (or built-in upper/lower case checks) demonstrates character-level ASCII manipulation.
 *
 * Interview Scenario:
 * "Asked at Accenture/Capgemini to test whether a candidate knows the ASCII offset (32) between upper and lower case letters."
 *
 * Example:
 *   Input:  str = "Hello World"
 *   Output: "hELLO wORLD"
 */
