// 16. Calculate frequency of character of string

/**
 * Pattern Used: Hashing / Frequency Map
 * Why this pattern: A hash map keyed by character accumulates counts in a single O(n) pass, the foundation for many string-statistics problems.
 *
 * Interview Scenario:
 * "Extremely common opening question (Amazon, Flipkart) that leads into follow-ups like 'find the most/least frequent character'."
 *
 * Example:
 *   Input:  str = "aabbbc"
 *   Output: { a: 2, b: 3, c: 1 }
 */
