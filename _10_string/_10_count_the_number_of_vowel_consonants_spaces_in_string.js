// 10. Count the number of vowel, consonants, spaces in string

/**
 * Pattern Used: Single-Pass Character Classification
 * Why this pattern: A single loop over the string classifying each character (vowel/consonant/space) via conditionals or a Set gives all three counts in O(n).
 *
 * Interview Scenario:
 * "A common entry-level interview/HR-round coding question at product-based startups to test basic string traversal and conditional logic."
 *
 * Example:
 *   Input:  str = "Hello World"
 *   Output: vowels = 3, consonants = 7, spaces = 1
 */
