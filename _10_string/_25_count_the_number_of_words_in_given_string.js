// 25. Count the number of words in given string

/**
 * Pattern Used: String Splitting / Single-Pass Counting
 * Why this pattern: Splitting on whitespace (after trimming/normalizing) and counting the resulting tokens gives the word count directly.
 *
 * Interview Scenario:
 * "A very common HR/basic coding round question (TCS NQT, Infosys) to test string normalization and split-based counting."
 *
 * Example:
 *   Input:  str = "  Hello   World  "
 *   Output: 2
 */
