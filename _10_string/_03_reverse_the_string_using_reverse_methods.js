// 3. Reverse the string using reverse methods
// LeetCode: https://leetcode.com/problems/reverse-string/

/**
 * Pattern Used: String Reversal (Built-in Methods)
 * Why this pattern: Leveraging split/reverse/join (or Array.from().reverse()) is the idiomatic JavaScript way to reverse a string when built-in utilities are allowed.
 *
 * Interview Scenario:
 * "A common opening question at almost every company to confirm basic JS string/array API fluency before moving to harder variants like in-place reversal."
 *
 * Example:
 *   Input:  str = "hello"
 *   Output: "olleh"
 */
