// 8. Remove given character from string

/**
 * Pattern Used: String Filtering (Character Removal)
 * Why this pattern: Iterating through the string and skipping/excluding the target character (or using split/filter/join) removes all its occurrences in one linear pass.
 *
 * Interview Scenario:
 * "Basic string manipulation question asked at service-based companies (TCS/Infosys/Wipro) to test filtering logic and edge-case handling for empty strings or absent characters."
 *
 * Example:
 *   Input:  str = "banana", char = "a"
 *   Output: "bnn"
 */
