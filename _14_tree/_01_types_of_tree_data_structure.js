// 1. Types of the tree data structure

/**
 * Pattern Used: Tree Taxonomy (Conceptual Model)
 * Why this pattern: This file is a conceptual map of tree variants (general, binary, BST, AVL, red-black, B-tree, trie, segment tree) rather than an algorithm; understanding the taxonomy is the prerequisite for choosing the right traversal or balancing strategy later.
 *
 * Interview Scenario:
 * "Interviewers often open a tree round with 'walk me through the different kinds of trees and when you'd reach for each one' before diving into code, to gauge whether you pick structures deliberately instead of by memorized templates."
 *
 * Example:
 *   Input:  A generic hierarchy: general tree, binary tree, BST, AVL tree, red-black tree, B-tree, trie, segment/Fenwick tree
 *   Output: A classification list explaining the constraints and use-case of each variant (e.g. BST = ordered, AVL/red-black = self-balancing BST, B-tree = disk-oriented multiway tree, trie = prefix tree over strings)
 */
