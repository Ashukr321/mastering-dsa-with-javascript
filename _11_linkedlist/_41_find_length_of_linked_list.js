// 41. Find the length of the linked list
/**
 * Pattern Used: Iterative Traversal
 * Why this pattern: A counter is incremented once per node while walking from head to null, since there is no length property maintained on the list itself.
 *
 * Interview Scenario:
 * "A simple building-block question, often a precursor to problems like finding the k-th node from the end or the intersection point of two lists."
 *
 * Example:
 *   Input:  list: 1 -> 2 -> 3 -> null
 *   Output: 3
 */
