// 10. Search the node in the singly linked list
/**
 * Pattern Used: Linear Search via Traversal
 * Why this pattern: Since a linked list has no random access/indexing, finding a value requires a sequential scan comparing each node's data to the target until a match or the end is reached.
 *
 * Interview Scenario:
 * "Asked to test whether candidates understand why binary search cannot be applied to a linked list even if the data is sorted."
 *
 * Example:
 *   Input:  list: 1 -> 2 -> 3 -> null, search(2)
 *   Output: true (found at index 1)
 */
