// 11. Print the sum of the all the node data
/**
 * Pattern Used: Iterative Traversal / Accumulator Pattern
 * Why this pattern: A single pass through the list accumulates a running total in a variable, since there is no indexed access to sum values in bulk.
 *
 * Interview Scenario:
 * "A simple aggregation question used as a lead-in before harder problems, checking comfort with traversal plus an accumulator."
 *
 * Example:
 *   Input:  list: 1 -> 2 -> 3 -> null
 *   Output: 6
 */
