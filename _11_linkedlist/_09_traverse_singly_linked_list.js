// 9. Traverse the singly linked list
/**
 * Pattern Used: Iterative Traversal
 * Why this pattern: Visiting every node exactly once by following next references from head until null is the foundational access pattern every other linked list operation builds on.
 *
 * Interview Scenario:
 * "The very first linked list question in almost every interview: 'print all elements of the list' — used to confirm the candidate understands the next-pointer chain."
 *
 * Example:
 *   Input:  list: 1 -> 2 -> 3 -> null
 *   Output: printed sequence: 1 2 3
 */
